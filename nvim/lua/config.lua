local M = {}

-- General Settings
table.insert(M, {})

-- LSP
table.insert(M, {
	lsp_enabled = true,
	-- lsp_autostart_blacklist = { "sumneko_lua" },
})

-- Completion
table.insert(M, {})

-- Treesitter
table.insert(M, {})

return M
