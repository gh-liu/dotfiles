{
  "name": "pi-extensions",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:subagent:live": "node -e 'if (!process.env.DEEPSEEK_API_KEY) { console.error(\"DEEPSEEK_API_KEY is required\"); process.exit(1) }' && PI_SUBAGENT_LIVE_TEST=1 npx -y node@22 node_modules/vitest/vitest.mjs run subagent/live.test.ts",
    "test:tui": "bash archive/tui.e2e.sh"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "@earendil-works/pi-tui": "0.84.1",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
