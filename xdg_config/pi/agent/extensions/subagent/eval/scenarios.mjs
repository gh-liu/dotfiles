const reviewerOutcome = [
  "(?:default|默认).{0,100}(?:TTL|60.second|regression|test|回归|测试)",
  "(?:TTL|regression|test|回归|测试).{0,100}(?:default|默认)",
];

export const scenarios = [
  {
    id: "simple-lookup",
    description: "A single-file lookup should stay in the parent.",
    quick: true,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    expectation: {
      maxSubagentCalls: 0,
      parentToolCounts: { read: { min: 1 } },
    },
    prompt: "Read src/session.js and tell me the value of DEFAULT_TTL_SECONDS. This is a single-file lookup. Do not modify files.",
  },
  {
    id: "local-discovery",
    description: "Bounded multi-file discovery should route to scout.",
    quick: true,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: { requiredAgents: ["scout"], maxSubagentCalls: 1 },
    prompt: "Map the session creation lifecycle across src/session.js, src/handler.js, and tests. Identify the call flow, TTL units, and likely change seam. This is a bounded, read-only multi-file discovery task. Do not modify files.",
  },
  {
    id: "external-research",
    description: "Source-heavy web research should route to researcher without duplicate parent searches.",
    quick: true,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: {
      requiredAgents: ["researcher"],
      maxSubagentCalls: 1,
      parentToolCounts: { web_search: { max: 0 } },
    },
    prompt: "Research current authoritative Node.js guidance for testing time-dependent code with the built-in node:test runner. Use multiple primary sources, assess source quality, and return URLs plus a recommendation for this repository. This is source-heavy external research; do not modify files.",
  },
  {
    id: "independent-review",
    description: "An explicit independent diff review should route to reviewer and find the test gap.",
    quick: true,
    repeats: 1,
    fixture: "fixed-missing-test",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: { requiredAgents: ["reviewer"], maxSubagentCalls: 1 },
    expectation: { requiredAgents: ["reviewer"], maxSubagentCalls: 1, finalAny: reviewerOutcome },
    prompt: "Independently review the current uncommitted TTL fix against plan.md. Inspect the diff and tests, look for concrete correctness or regression gaps, and report findings with severity and file/line evidence. Do not modify files.",
  },
  {
    id: "small-coherent-implementation",
    description: "A small coherent implementation should stay in the parent and pass tests.",
    quick: true,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    expectation: { maxSubagentCalls: 0 },
    prompt: "Implement plan.md completely. This is one small coherent change: fix TTL units, add the requested regression test, and run the tests. Work directly without delegation. Do not commit.",
  },
  {
    id: "explicit-worker",
    description: "Explicit delegated implementation should route to worker and pass tests.",
    quick: true,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    hardExpectation: { requiredAgents: ["worker"], maxSubagentCalls: 1 },
    expectation: { requiredAgents: ["worker"], maxSubagentCalls: 1 },
    prompt: "Delegate the implementation in plan.md to an implementation subagent. Require it to fix TTL units, add the regression test, run tests, and return a concise handoff. Verify the settled result, but do not redo its work or commit.",
  },
  {
    id: "high-impact-decision",
    description: "An unresolved compatibility judgment should route to oracle.",
    quick: false,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: { requiredAgents: ["oracle"], maxSubagentCalls: 1 },
    prompt: "Do not edit. An unresolved high-impact compatibility decision remains: while fixing TTL, should expiresAt stay an epoch-millisecond number or become an ISO string? Inspect repository constraints, then delegate exactly one authoritative read-only judgment on this decision and synthesize it. This is not a general code review.",
  },
  {
    id: "combo-implementation-review",
    description: "Discovery, implementation, and independent review should compose in order.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    hardExpectation: { agentOrder: ["scout", "worker", "reviewer"] },
    expectation: { agentOrder: ["scout", "worker", "reviewer"] },
    prompt: "Complete plan.md using staged delegation. First delegate bounded local discovery to map the change seam. After that handoff, delegate implementation to a writing-capable subagent. After tests pass, delegate an independent read-only review of the resulting diff. Address any blocking finding, but do not commit. Do not replace these stages with parent self-review.",
  },
  {
    id: "combo-evidence-decision",
    description: "Local and external evidence gathering should precede the oracle decision.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout", "researcher", "oracle"],
      agentsBefore: { agents: ["scout", "researcher"], before: "oracle" },
    },
    expectation: {
      requiredAgents: ["scout", "researcher", "oracle"],
      agentsBefore: { agents: ["scout", "researcher"], before: "oracle" },
    },
    prompt: "Resolve this design question without editing: should Lantern keep expiresAt as an epoch-millisecond number or change it to an ISO string while fixing TTL? First delegate bounded local discovery of code/API constraints and source-heavy external research of authoritative JavaScript interoperability guidance as independent evidence-gathering tasks. Once both handoffs settle, delegate the final high-impact compatibility judgment to an authoritative decision subagent. Synthesize the decision and citations.",
  },
  {
    id: "persistent-follow-up",
    description: "A persistent scout should support wait, follow-up, and close lifecycle actions.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      actionSequence: [
        { action: "start" },
        { action: "wait" },
        { action: "send", mode: "follow_up" },
        { action: "wait" },
        { action: "close" },
      ],
    },
    expectation: {
      requiredAgents: ["scout"],
      actionSequence: [
        { action: "start" },
        { action: "wait" },
        { action: "send", mode: "follow_up" },
        { action: "wait" },
        { action: "close" },
      ],
    },
    prompt: "Exercise one persistent read-only subagent runtime to investigate TTL behavior. Start it with an initial request to map createSession. Wait for completion. Send a follow_up asking it to compare the tests with plan.md, wait again, then close the runtime. Use the subagent lifecycle rather than separate one-shot runs. Do not modify files.",
  },
];
