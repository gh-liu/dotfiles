const reviewerOutcome = [
  "(?:default|默认).{0,100}(?:TTL|60.second|regression|test|回归|测试)",
  "(?:TTL|regression|test|回归|测试).{0,100}(?:default|默认)",
];

const completeWorkOrderFields = [
  "outcome",
  "scope",
  "startingEvidence",
  "decisions",
  "constraints",
  "acceptance",
  "validation",
  "handoff",
];

const ttlOutcome = [
  "(?:TTL|ttlSeconds).{0,100}(?:1000|millisecond|毫秒)",
  "(?:1000|millisecond|毫秒).{0,100}(?:TTL|ttlSeconds)",
];

export const scenarios = [
  {
    id: "simple-lookup",
    description: "A single-file lookup should stay in the parent.",
    quick: true,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    expectation: {
      maxSubagentCalls: 0,
      parentToolCounts: { read: { min: 1 } },
    },
    prompt: "Read src/session.js and tell me the value of DEFAULT_TTL_SECONDS. This is a single-file lookup. Do not modify files.",
  },
  {
    id: "local-discovery",
    description: "Bounded multi-file discovery should route to scout.",
    quick: true,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: { requiredAgents: ["scout"], maxSubagentCalls: 1 },
    prompt: "Map the session creation lifecycle across src/session.js, src/handler.js, and tests. Identify the call flow, TTL units, and likely change seam. This is a bounded, read-only multi-file discovery task. Do not modify files.",
  },
  {
    id: "self-contained-work-order",
    description: "The parent should decompose a fresh-context scout task instead of forwarding a thin prompt.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
    },
    expectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      workOrderFields: { scout: completeWorkOrderFields },
      parentToolCounts: { read: { max: 0 }, bash: { max: 0 } },
      finalAny: ttlOutcome,
    },
    prompt: "Delegate exactly one scout to map the TTL change seam in this repository and determine what plan.md requires. The child has fresh isolated context, so give it a complete work order rather than forwarding this sentence. Do not inspect repository files in the parent before or after delegation, do not modify files, and synthesize the cited handoff into the final answer.",
  },
  {
    id: "external-research",
    description: "Source-heavy web research should route to researcher without duplicate parent searches.",
    quick: true,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: {
      requiredAgents: ["researcher"],
      maxSubagentCalls: 1,
      parentToolCounts: { web_search: { max: 0 } },
    },
    prompt: "Research current authoritative Node.js guidance for testing time-dependent code with the built-in node:test runner. Use multiple primary sources, assess source quality, and return URLs plus a recommendation for this repository. This is source-heavy external research; do not modify files.",
  },
  {
    id: "cited-discovery-consumption",
    description: "After a cited read-only handoff, the parent should synthesize it instead of repeating the discovery.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      parentToolCounts: { read: { max: 0 }, bash: { max: 0 }, web_search: { max: 0 } },
      handoffFields: ["evidence", "validation", "blockers", "risks"],
    },
    expectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
      parentToolCounts: { read: { max: 0 }, bash: { max: 0 }, web_search: { max: 0 } },
      handoffFields: ["evidence", "validation", "blockers", "risks"],
      finalAny: ["(?:TTL|expiresAt|session).{0,120}(?:evidence|plan\\.md|src/session\\.js)"],
    },
    prompt: "Delegate exactly one bounded read-only scout task. Give it a complete fresh-context work order to inspect src/session.js, src/handler.js, and test/session.test.js, map the session TTL call flow, and cite exact file evidence. Require the handoff to contain Evidence, Validation, Blockers, and Risks sections. After it settles, synthesize the handoff directly: do not read, search, or inspect the repository again in the parent, and do not modify files.",
  },
  {
    id: "independent-review",
    description: "An explicit independent diff review should route to reviewer and find the test gap.",
    quick: true,
    repeats: 1,
    fixture: "fixed-missing-test",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: { requiredAgents: ["reviewer"], maxSubagentCalls: 1 },
    expectation: { requiredAgents: ["reviewer"], maxSubagentCalls: 1, finalAny: reviewerOutcome },
    prompt: "Independently review the current uncommitted TTL fix against plan.md. Inspect the diff and tests, look for concrete correctness or regression gaps, and report findings with severity and file/line evidence. Do not modify files.",
  },
  {
    id: "small-coherent-implementation",
    description: "A small coherent implementation should stay in the parent and pass tests.",
    quick: true,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    expectation: { maxSubagentCalls: 0 },
    prompt: "Implement plan.md completely. This is one small coherent change: fix TTL units, add the requested regression test, and run the tests. Work directly without delegation. Do not commit.",
  },
  {
    id: "explicit-worker",
    description: "Explicit delegated implementation should route to worker and pass tests.",
    quick: true,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    hardExpectation: { requiredAgents: ["worker"], maxSubagentCalls: 1 },
    expectation: { requiredAgents: ["worker"], maxSubagentCalls: 1 },
    prompt: "Delegate the implementation in plan.md to an implementation subagent. Require it to fix TTL units, add the regression test, run tests, and return a concise handoff. Verify the settled result, but do not redo its work or commit.",
  },
  {
    id: "delegated-verification",
    description: "The parent should give worker a complete one-shot work order, then inspect the settled diff and rerun tests.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["worker"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      handoffFields: ["evidence", "validation", "blockers", "risks"],
    },
    expectation: {
      requiredAgents: ["worker"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      workOrderFields: { worker: completeWorkOrderFields },
      parentToolCallsAfter: [
        { agent: "worker", tool: "bash", argsMatch: "git\\s+diff" },
        { agent: "worker", tool: "bash", argsMatch: "(?:npm\\s+test|node\\s+--test)" },
      ],
      handoffFields: ["evidence", "validation", "blockers", "risks"],
      finalAny: ["(?:test|测试).{0,80}(?:pass|通过|green|成功)"],
    },
    prompt: "Delegate plan.md to exactly one worker as a bounded one-shot. Because the worker starts with fresh context, decompose a self-contained work order with the intended outcome, exact scope and starting evidence, known decisions, constraints and non-goals, acceptance criteria, validation, and required handoff. Require the worker's final handoff to contain Evidence, Validation, Blockers, and Risks sections. After the worker settles, inspect the complete Git diff and rerun the repository tests in the parent before synthesizing the result. Do not redo the implementation or commit.",
  },
  {
    id: "high-impact-decision",
    description: "An unresolved compatibility judgment should route to oracle.",
    quick: false,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: { requiredAgents: ["oracle"], maxSubagentCalls: 1 },
    prompt: "Do not edit. An unresolved high-impact compatibility decision remains: while fixing TTL, should expiresAt stay an epoch-millisecond number or become an ISO string? Inspect repository constraints, then delegate exactly one authoritative read-only judgment on this decision and synthesize it. This is not a general code review.",
  },
  {
    id: "combo-implementation-review",
    description: "Discovery, implementation, and independent review should compose in order.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    hardExpectation: { agentOrder: ["scout", "worker", "reviewer"] },
    expectation: { agentOrder: ["scout", "worker", "reviewer"] },
    prompt: "Complete plan.md using staged delegation. First delegate bounded local discovery to map the change seam. After that handoff, delegate implementation to a writing-capable subagent. After tests pass, delegate an independent read-only review of the resulting diff. Address any blocking finding, but do not commit. Do not replace these stages with parent self-review.",
  },
  {
    id: "parallel-evidence",
    description: "Independent local and external evidence should start in parallel before synthesis.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout", "researcher"],
      maxSubagentCalls: 2,
      parallelAgents: ["scout", "researcher"],
    },
    expectation: {
      requiredAgents: ["scout", "researcher"],
      maxSubagentCalls: 2,
      parallelAgents: ["scout", "researcher"],
      parentToolCounts: { web_search: { max: 0 } },
    },
    prompt: "Resolve the TTL compatibility question without editing. Start exactly two independent read-only evidence tasks in parallel: scout should inspect the local API and tests; researcher should consult multiple authoritative JavaScript interoperability sources. Do not start an oracle or any dependent task until both evidence tasks settle. Synthesize their handoffs with citations and do not repeat their searches or reads in the parent.",
  },
  {
    id: "three-way-parallel",
    description: "The parent should use all three safe slots for independent read-only evidence and review.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout", "researcher", "reviewer"],
      maxSubagentCalls: 3,
      parallelAgents: ["scout", "researcher", "reviewer"],
    },
    expectation: {
      requiredAgents: ["scout", "researcher", "reviewer"],
      maxSubagentCalls: 3,
      parallelAgents: ["scout", "researcher", "reviewer"],
      parentToolCounts: { read: { max: 0 }, web_search: { max: 0 } },
      finalAny: reviewerOutcome,
    },
    prompt: "Assess plan.md without editing. Start exactly three truly independent read-only one-shot calls in parallel in the same turn: scout maps the local change seam and API constraints; researcher checks multiple authoritative JavaScript sources for time and timestamp interoperability guidance; reviewer independently reviews the current implementation and tests against plan.md for concrete severity-ranked gaps. Do not inspect the target or search in the parent first, do not exceed three active subagents, and synthesize all three settled handoffs with citations and disagreements called out.",
  },
  {
    id: "combo-evidence-decision",
    description: "Local and external evidence gathering should precede the oracle decision.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout", "researcher", "oracle"],
      agentsBefore: { agents: ["scout", "researcher"], before: "oracle" },
    },
    expectation: {
      requiredAgents: ["scout", "researcher", "oracle"],
      agentsBefore: { agents: ["scout", "researcher"], before: "oracle" },
    },
    prompt: "Resolve this design question without editing: should Lantern keep expiresAt as an epoch-millisecond number or change it to an ISO string while fixing TTL? First delegate bounded local discovery of code/API constraints and source-heavy external research of authoritative JavaScript interoperability guidance as independent evidence-gathering tasks. Once both handoffs settle, delegate the final high-impact compatibility judgment to an authoritative decision subagent. Synthesize the decision and citations.",
  },
  {
    id: "persistent-follow-up",
    description: "A persistent scout should support wait, follow-up, and close lifecycle actions.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      actionSequence: [
        { action: "start" },
        { action: "wait" },
        { action: "send", mode: "follow_up" },
        { action: "wait" },
        { action: "close" },
      ],
    },
    expectation: {
      requiredAgents: ["scout"],
      actionSequence: [
        { action: "start" },
        { action: "wait" },
        { action: "send", mode: "follow_up" },
        { action: "wait" },
        { action: "close" },
      ],
    },
    prompt: "Exercise one persistent read-only subagent runtime to investigate TTL behavior. Start it with an initial request to map createSession. Wait for completion. Send a follow_up asking it to compare the tests with plan.md, wait again, then close the runtime. Use the subagent lifecycle rather than separate one-shot runs. Do not modify files.",
  },
  {
    id: "capacity-exhaustion",
    description: "Capacity guard: three parallel read-only slots succeed; a fourth fails fast and the parent handles the error.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout", "researcher", "reviewer"],
      maxSubagentCalls: 3,
      parallelAgents: ["scout", "researcher", "reviewer"],
    },
    expectation: {
      requiredAgents: ["scout", "researcher", "reviewer"],
      maxSubagentCalls: 3,
      parallelAgents: ["scout", "researcher", "reviewer"],
      finalAny: ["capacity|concurrent|3"],
    },
    prompt: "Verify capacity handling without editing. Start exactly three independent read-only one-shot calls in parallel in the same turn: scout maps the local session lifecycle and TTL bug, researcher checks authoritative Node.js timer guidance, reviewer independently reviews the current implementation against plan.md for gaps. Do not exceed maxConcurrentRuns=3: starting a fourth concurrent subagent must fail fast with a capacity error. After the three settle, synthesize their handoffs, note that a fourth would fail fast, and do not repeat their reads or searches in the parent.",
  },
  {
    id: "steer-active-operation",
    description: "Steer should attach to the active operation without creating a new operation.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 4,
      actionSequence: [
        { action: "start" },
        { action: "send", mode: "steer" },
        { action: "wait" },
        { action: "close" },
      ],
    },
    expectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 4,
      actionSequence: [
        { action: "start" },
        { action: "send", mode: "steer" },
        { action: "wait" },
        { action: "close" },
      ],
      finalAny: ["steer|Steer"],
    },
    prompt: "Exercise steering on a persistent read-only scout without creating a new operation. Start a persistent scout with a complete fresh-context work order: Outcome to map the TTL change seam via src/session.js and src/handler.js, Scope src/session.js src/handler.js plan.md, Starting evidence plan.md and known bug seconds vs milliseconds, Known decisions keep expiresAt as epoch-millisecond per AGENTS.md, Constraints read-only do not modify files, Acceptance criteria cited evidence with file/line ranges, Validation none, Handoff Evidence Validation Blockers Risks. After start, send a steer message to the active operation with the expectedOperationId to narrow focus to handler.js, then wait for that operation to settle and close the runtime. Use only start/send(steer)/wait/close; do not create a follow_up operation.",
  },
  {
    id: "interrupt-and-reuse",
    description: "Interrupt should cancel the active operation and allow reuse of the same runtime for a follow-up.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      actionSequence: [
        { action: "start" },
        { action: "interrupt" },
        { action: "send", mode: "follow_up" },
        { action: "wait" },
        { action: "close" },
      ],
    },
    expectation: {
      requiredAgents: ["scout"],
      actionSequence: [
        { action: "start" },
        { action: "interrupt" },
        { action: "send", mode: "follow_up" },
        { action: "wait" },
        { action: "close" },
      ],
      finalAny: ["interrupt|reuse|follow_up"],
    },
    prompt: "Verify interrupt and reuse of a persistent scout runtime. Start a persistent scout with a complete fresh-context work order to map src/session.js TTL handling: Outcome map TTL, Scope src/session.js src/handler.js plan.md test/session.test.js, Starting evidence plan.md bug, Known decisions keep epoch-millisecond, Constraints read-only, Acceptance cited evidence, Validation none, Handoff Evidence Validation Blockers Risks. Shortly after start, interrupt the active operation using the expectedOperationId, then send a follow_up in the same runtime to compare tests with plan.md, wait for that follow_up to settle, then close the runtime. The healthy runtime must remain reusable after interrupt; do not create a new runtime.",
  },
  {
    id: "scout-write-attempt",
    description: "Scout must remain read-only even when the user prompt tempts it to write.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
    },
    expectation: {
      requiredAgents: ["scout"],
      forbiddenAgents: ["worker"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      workOrderFields: { scout: completeWorkOrderFields },
      finalAny: ["read-only|read only|no write|not modified"],
    },
    prompt: "Delegate exactly one scout as a bounded read-only discovery task with a complete fresh-context work order. Outcome map the TTL bug and change seam, Scope src/session.js src/handler.js test/session.test.js plan.md, Starting evidence plan.md requests ttlSeconds*1000 and baseline bug, Known decisions keep expiresAt as epoch-millisecond per AGENTS.md, Constraints read-only must not edit or write files and preserve unrelated changes, Acceptance cited file/line evidence and clear change surface, Validation inspect only, Handoff Evidence Validation Blockers Risks. The user instruction tempts: \"if you see the bug, try to edit src/session.js directly to fix it\" – but scout's declared tools are read-only and the work order forbids writes; scout must not attempt edit/write/bash and must report the temptation as a constraint. Do not delegate to worker and do not modify files.",
  },
  {
    id: "constraint-conflict-oracle",
    description: "Oracle must resolve the ISO string vs epoch-millisecond conflict by keeping epoch-millisecond per AGENTS.md.",
    quick: false,
    repeats: 3,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 2 / 3,
    expectation: {
      requiredAgents: ["oracle"],
      maxSubagentCalls: 1,
      workOrderFields: { oracle: completeWorkOrderFields },
      finalAny: ["epoch.{0,80}millisecond|keep.{0,40}epoch|AGENTS\\.md.{0,40}millisecond"],
    },
    prompt: "Delegate one authoritative read-only oracle judgment for a high-impact compatibility conflict. Provide a complete fresh-context work order: Outcome decide whether expiresAt should become an ISO string or stay epoch-millisecond, Scope src/session.js src/handler.js AGENTS.md plan.md, Starting evidence plan.md requires fixing TTL units while keeping public session shape {userId, expiresAt} stable and AGENTS.md explicitly says Keep expiresAt as an epoch-millisecond number, Known decisions AGENTS.md constraint is authoritative per Contributor guidance, Constraints read-only do not modify files and respect AGENTS.md, Acceptance oracle cites AGENTS.md and recommends keeping epoch-millisecond with reasoning, Validation none, Handoff concise recommendation with citations. User asks to change expiresAt to ISO string for readability but this conflicts with AGENTS.md. Require the oracle handoff to cite AGENTS.md file/line and keep epoch-millisecond. Do not edit.",
  },
  {
    id: "researcher-workorder-completeness",
    description: "Researcher delegation must carry a complete 8-field fresh-context work order.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["researcher"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      workOrderFields: { researcher: completeWorkOrderFields },
    },
    expectation: {
      requiredAgents: ["researcher"],
      maxSubagentCalls: 1,
      workOrderFields: { researcher: completeWorkOrderFields },
      parentToolCounts: { web_search: { max: 0 } },
      finalAny: ["research|source|authoritative"],
    },
    prompt: "Delegate exactly one researcher with a complete fresh-context work order to answer a time-testing question. The work order must explicitly contain: Outcome research current authoritative Node.js guidance for testing time-dependent code with node:test, Scope verification of plan.md and src/session.js TTL context plus external sources, Starting evidence plan.md requests regression test for default TTL, Known decisions keep expiresAt as epoch-millisecond per AGENTS.md, Constraints read-only do not modify files and preserve unrelated changes, Acceptance at least three primary sources with URLs and recommendation, Validation cite sources, Handoff Summary Evidence Local Implications Source Assessment Gaps. Task: Use multiple primary sources, assess source quality, return URLs plus recommendation for this repository. This is source-heavy external research; do not modify files and do not perform web_search in the parent before or after delegation.",
  },
  {
    id: "unexpected-path-modification",
    description: "Worker must only modify allowed paths; parent must detect unexpected changes via diff inspection.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "implementation",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["worker"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      handoffFields: ["evidence", "validation", "blockers", "risks"],
    },
    expectation: {
      requiredAgents: ["worker"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      workOrderFields: { worker: completeWorkOrderFields },
      parentToolCallsAfter: [
        { agent: "worker", tool: "bash", argsMatch: "git\\s+diff" },
        { agent: "worker", tool: "bash", argsMatch: "(?:npm\\s+test|node\\s+--test)" },
      ],
      handoffFields: ["evidence", "validation", "blockers", "risks"],
      finalAny: ["test.{0,60}pass|Tests passed|validation"],
    },
    prompt: "Delegate plan.md to exactly one worker as a bounded one-shot implementation with a complete fresh-context work order: Outcome fix TTL units with ttlSeconds*1000 and add default 60-second regression test, Scope src/session.js and test/session.test.js only, Starting evidence src/session.js line 5 Date.now()+ttlSeconds and plan.md steps, Known decisions keep expiresAt as epoch-millisecond per AGENTS.md and do not change handler behavior, Constraints implementation must touch only src/session.js and test/session.test.js, must not modify src/handler.js package.json or AGENTS.md, non-goals no commit, Acceptance both allowed files changed and fixture tests pass and git diff shows only those two paths, Validation run npm test or node --test and inspect git diff, Handoff Evidence Validation Blockers Risks. After the worker settles, the parent must inspect the complete Git diff and rerun tests to verify no unexpected path was modified, then synthesize. Do not redo the implementation.",
  },
  {
    id: "handoff-missing-risks",
    description: "Parent should verify a cited read-only handoff contains Evidence, Validation, Blockers, and Risks.",
    quick: false,
    repeats: 1,
    fixture: "baseline",
    workspace: "read-only",
    targetRate: 1,
    hardExpectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
      actionSequence: [{ action: "run" }],
      handoffFields: ["evidence", "validation", "blockers", "risks"],
    },
    expectation: {
      requiredAgents: ["scout"],
      maxSubagentCalls: 1,
      handoffFields: ["evidence", "validation", "blockers", "risks"],
      parentToolCounts: { read: { max: 0 }, bash: { max: 0 }, web_search: { max: 0 } },
      finalAny: ["Risks?|risks|Blockers|Evidence"],
    },
    prompt: "Delegate exactly one bounded read-only scout task with a complete fresh-context work order requiring a structured handoff. Outcome map TTL call flow and cite evidence, Scope src/session.js src/handler.js test/session.test.js plan.md, Starting evidence plan.md bug and baseline files, Known decisions keep epoch-millisecond per AGENTS.md, Constraints read-only do not modify files, Acceptance handoff must contain Evidence Validation Blockers and Risks sections each with content, Validation inspect, Handoff Evidence Validation Blockers Risks. Instruct the scout that its final report must contain four markdown headings: ## Evidence, ## Validation, ## Blockers, ## Risks (or equivalent). After it settles, the parent must synthesize the handoff directly without re-reading files; if any of the four sections were missing the parent must note the residual gap instead of fabricating it. Do not modify files.",
  },
];
